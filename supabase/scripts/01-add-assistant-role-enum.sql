-- =============================================================================
-- الخطوة 1 — شغّل هذا الملف وحده (سطر واحد فقط — لا تضف شيئاً بعده)
-- =============================================================================
-- ⚠️ لا تُشغّل أي SELECT أو سكربت آخر في نفس الـ Run
--    PostgreSQL يمنع استخدام قيمة enum جديدة قبل COMMIT
--
-- بعد النجاح، شغّل Run جديداً:
--   02-apply-assistant-authorization.sql
--
-- (اختياري) للتحقق بعد النجاح شغّل في Run منفصل:
--   01-verify-assistant-enum.sql

ALTER TYPE public.user_role ADD VALUE IF NOT EXISTS 'assistant';
